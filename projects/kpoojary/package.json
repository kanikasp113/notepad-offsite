{
  "name": "mutanotes",
  "version": "1.0.0",
  "description": "Notes that are never exactly what you wrote — every view mutates the text",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
